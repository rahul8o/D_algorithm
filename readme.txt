

The program comprises a parser which reads a iscas 85 benchmark circuit and maps it to 
a logical boolean circuit. Then the program generates a list of dominant faults and 
generate the test pattern to detect the faults.
